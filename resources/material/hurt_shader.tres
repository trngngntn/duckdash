[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;
uniform bool hurt = false;

void fragment(){
	vec4 color = texture(TEXTURE, UV);
	if(hurt && color.a == 1.0){
		COLOR = vec4(mix(color.rgb, vec3(1.0,1.0,1.0), 0.75), 0.9);
	} else {
		COLOR = color
	}
}"
